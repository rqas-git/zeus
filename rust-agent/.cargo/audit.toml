[advisories]
# Temporary exception to keep FFF search enabled while its `heed` dependency
# still pulls in unmaintained `bincode` 1.3.3. See docs/architecture/SECURITY.md.
ignore = ["RUSTSEC-2025-0141"]
